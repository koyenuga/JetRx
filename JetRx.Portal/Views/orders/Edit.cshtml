@model JetRx.Entities.order

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>prescription</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            <label class="control-label col-md-2"> First Name</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.customer_prescription.Customer.firstname, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.customer_prescription.Customer.firstname, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2"> Last Name</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.customer_prescription.Customer.firstname, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.customer_prescription.Customer.firstname, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">Prescription</label>
            <div class="col-md-10">
                <img src="/orders/GetImage/@Model.customer_prescription.Prescription.image_id" />
            </div>
        </div>
        @*<div class="form-group">
            <label class="control-label col-md-2">Insurance</label>
            <div class="col-md-10">
                <img src="/orders/GetImage/@Model.customer_prescription.Customer.Insurance.image_id" />
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">Identification</label>
            <div class="col-md-10">
                <img src="/orders/GetImage/@Model.customer_prescription.Customer.Identification.image_id" />
            </div>
        </div>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
